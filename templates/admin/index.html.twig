{% extends 'base.html.twig' %}

{% block title %} BackOffice {% endblock %}

{% block body %}

    <div class="container">
            <h1 class="display-4 text-center my-5"> BACK OFFICE </h1>

            <h4 class="text-center mb-5"> 
                Bienvenue

            {# Si l'internaute est connecté, alors on affiche son username en piochant directement dans la session (app.user)#}
            {% if app.user %}

                <span style="color: #0006BD"> {{ app.user.username }}</span>

            {% endif %}
                dans le back-office
            </h4>

            <div class="text-center">

                <a href="{{ path('admin_articles') }}" class="col-md-4 btn btn-secondary mb-2 ">GESTION DES ARTICLES</a>

                <a href="{{ path('admin_category') }}" class="col-md-4 btn btn-dark mb-2">GESTION DES CATEGORIES</a>

                <a href="{{ path('admin_comments') }}" class="col-md-4 btn btn-dark">GESTION DES COMMENTAIRES</a>

                <a href="" class="col-md-4 btn btn-secondary">GESTION DES MEMBRES</a>

            </div>
    </div>  

{% endblock %}
